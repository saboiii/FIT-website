# Tasks: Discoverable Reset-to-Defaults

## 1. Global reset discoverability
- [ ] 1.1 Surface a "Reset to defaults" control in generic mode (and keep in advanced)
- [ ] 1.2 Reuse existing `defaultVisual`/`defaultLighting`/`defaultPrintability`

## 2. Per-setting reset
- [ ] 2.1 Add a per-field "reset to default" affordance for individual settings
- [ ] 2.2 Resetting one field leaves the others unchanged

## 3. Verify
- [ ] 3.1 Manual: in generic mode, reset returns all values to defaults
- [ ] 3.2 Manual: per-field reset reverts only that field
